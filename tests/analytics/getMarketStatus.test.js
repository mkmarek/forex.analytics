var assert = require('assert'),
  chai = require('chai'),
  expect = chai.expect,
  should = chai.should(),
  analytics = require('../../index'),
  candlesticks = require('../candlesticks');


describe('analytics', function() {
  describe('#getMarketStatus', function() {
    it('should be done later', function() {});
  });
});
