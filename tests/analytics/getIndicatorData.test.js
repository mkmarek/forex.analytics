var assert = require('assert'),
  chai = require('chai'),
  expect = chai.expect,
  should = chai.should(),
  analytics = require('../../build/Release/analytics.node'),
  candlesticks = require('../candlesticks');


describe('analytics', function() {
  describe('#getIndicatorData', function() {
    it('should be done later', function() {});
  });
});
